[package]
name = "core-domain"
version = "0.1.0"
edition = "2021"

[dependencies]
toml = "0.8"
walkdir = "2.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
chrono = { version = "0.4", features = ["serde"] }
