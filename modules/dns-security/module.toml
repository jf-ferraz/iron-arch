id = "dns-security"
name = "DNS Security"
description = "Encrypted DNS-over-TLS via Stubby for DNS privacy"
kind = "SecurityHardening"

packages = ["stubby"]
aur_packages = []
conflicts = []
depends = []
dotfiles = []
priority = 4
requires_root = true

[hooks]
post_install = "hooks/install.sh"
pre_uninstall = "hooks/uninstall.sh"
status_check = "hooks/status.sh"
