# Fail2ban Module
# Intrusion prevention framework

id = "fail2ban"
name = "Fail2ban"
description = "Intrusion prevention - bans IPs with too many failed login attempts"
kind = "SecurityHardening"

packages = ["fail2ban"]
aur_packages = []
conflicts = []
depends = ["ufw"]
dotfiles = []
priority = 2
requires_root = true

[hooks]
post_install = "hooks/install.sh"
pre_uninstall = "hooks/uninstall.sh"
status_check = "hooks/status.sh"
